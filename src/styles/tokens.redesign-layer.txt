/* ==== REDESIGN LAYER — "Sunlit Clay" =====================================================
   Appended by src/tools/append-redesign-tokens.mjs (idempotent, between the BEGIN/END markers).
   The generated baseline above is untouched. Brand colours below are the SOURCE's own,
   read from audit/design-baseline.json (count = occurrences in the computed capture):
     #f9e200 sun (background, 200)      #fbed6b pale sun (background, 32)
     #fe8b19 ember (declared in CSS, 260) #ffd1a3 peach (background, 24)
     #f5c96c gold (text, 324)            #074b44 pine (text 296, border 148)
     #365207 olive (text, 96)            #291b5f dusk (text, 138)
     --c-6 #231f20 ink · --c-4 #55575c · --c-7 #666a70 · --c-3 #ffffff (generated above)
   The "clay" surfaces and shadow tones are NEW to the redesign (neumorphism needs a mid-light
   solid ground and two shadow tones derived from it); they are the only invented colours. */
:root {
  color-scheme: light;

  /* brand (source) */
  --ink: var(--c-6);
  --ink-2: var(--c-4);
  --ink-3: color-mix(in oklab, var(--c-7) 80%, var(--c-6)); /* the source grey (--c-7) deepened: alone it is 4.39:1 on clay, under AA */
  --paper: var(--c-3);
  --sun: #f9e200;
  --sun-pale: #fbed6b;
  --ember: #fe8b19;
  --peach: #ffd1a3;
  --gold: #f5c96c;
  --pine: #074b44;
  --olive: #365207;
  --dusk: #291b5f;

  /* clay surfaces (redesign) */
  --clay: #eee6d8;
  --clay-hi: #f7f1e6;
  --clay-lo: #e2d8c6;
  --clay-deep: #d8ccb6;
  --clay-light: #fffaf2;
  --clay-shade: #c9bba2;
  --clay-shade-2: #b3a386;

  /* derived tones — mixed from the palette above, never re-typed */
  --pine-2: color-mix(in oklab, var(--pine) 78%, var(--paper));
  --pine-ink: color-mix(in oklab, var(--pine) 88%, var(--ink));
  --sun-2: color-mix(in oklab, var(--sun) 70%, var(--ember));
  --ember-ink: color-mix(in oklab, var(--ember) 48%, var(--ink)); /* 48% not 55%: the sativa badge clears AA */
  --dusk-2: color-mix(in oklab, var(--dusk) 70%, var(--paper));
  --olive-2: color-mix(in oklab, var(--olive) 75%, var(--paper));
  --ink-glass: color-mix(in oklab, var(--ink) 72%, transparent); /* 72%: white text on it clears AA even over a white photo */
  --paper-glass: color-mix(in oklab, var(--paper) 62%, transparent);
  --clay-glass: color-mix(in oklab, var(--clay-hi) 72%, transparent);

  /* strain type → colour (source badges use these families) */
  --strain-indica: var(--dusk);
  --strain-sativa: var(--ember-ink);
  --strain-hybrid: var(--pine);
  --strain-cbd: var(--olive);
  --strain-default: var(--ink-2);

  /* neumorphic depth */
  --rim: color-mix(in oklab, var(--clay-light) 70%, transparent);
  --shade-soft: color-mix(in oklab, var(--clay-shade) 75%, transparent);
  --shade-deep: color-mix(in oklab, var(--clay-shade-2) 60%, transparent);
  --neu-raise-sm: -4px -4px 10px var(--clay-light), 5px 6px 14px var(--shade-soft);
  --neu-raise: -9px -9px 20px var(--clay-light), 11px 13px 28px var(--shade-soft), 0 1px 0 var(--rim) inset;
  --neu-raise-lg: -16px -16px 36px var(--clay-light), 22px 26px 52px var(--shade-deep), 0 1px 0 var(--rim) inset;
  --neu-inset: inset 6px 6px 14px var(--shade-soft), inset -6px -6px 14px var(--clay-light);
  --neu-inset-sm: inset 3px 3px 7px var(--shade-soft), inset -3px -3px 7px var(--clay-light);
  --neu-pressed: inset 8px 8px 18px var(--shade-deep), inset -7px -7px 16px var(--clay-light);
  --lift-shadow: 0 28px 44px -18px color-mix(in oklab, var(--ink) 45%, transparent);
  --cutout-shadow: drop-shadow(0 22px 22px color-mix(in oklab, var(--ink) 34%, transparent)) drop-shadow(0 4px 6px color-mix(in oklab, var(--ink) 22%, transparent));
  --glow-sun: 0 0 0 4px color-mix(in oklab, var(--sun) 55%, transparent), 0 0 0 7px color-mix(in oklab, var(--pine) 70%, transparent);

  /* type (Fraunces display is new; Poppins is the source body face) */
  --font-serif: "Fraunces", "Iowan Old Style", "Palatino Linotype", Georgia, serif;
  --font-sans: "Poppins", var(--font-body);
  --step--1: clamp(0.8rem, 0.77rem + 0.15vw, 0.9rem);
  --step-0: clamp(1rem, 0.9rem + 0.25vw, 1.08rem); /* phone body text at 16px (was 15.2-15.5) */
  --step-1: clamp(1.15rem, 1.05rem + 0.5vw, 1.4rem);
  --step-2: clamp(1.4rem, 1.2rem + 1vw, 1.95rem);
  --step-3: clamp(1.8rem, 1.45rem + 1.8vw, 2.9rem);
  --step-4: clamp(2.3rem, 1.7rem + 3vw, 4.4rem);
  --step-5: clamp(2.8rem, 1.9rem + 4.6vw, 6.4rem);

  /* space & shape */
  --gutter: max(clamp(16px, 4vw, 48px), env(safe-area-inset-left), env(safe-area-inset-right)); /* clear of a landscape notch */
  --shell: 1320px;
  --radius-xs: 10px;
  --radius-sm: 16px;
  --radius: 26px;
  --radius-lg: 38px;
  --radius-pill: 999px;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --dur: 420ms;
}
